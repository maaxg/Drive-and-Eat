import {StyleSheet, Dimensions} from 'react-native'

export const styles = StyleSheet.create({
    scrollview:{
        flex: 1,
    },
    container: {
        position: 'absolute',
        alignItems: 'center',
        height: 500,
        width:'100%',
  
      },
     

})